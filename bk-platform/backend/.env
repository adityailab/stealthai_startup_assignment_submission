# ---- Core (required) ----
APP_NAME="BK Platform"
ENV=dev
SECRET_KEY=paste it here
ACCESS_TOKEN_EXPIRE_MINUTES=1440
DATABASE_URL=sqlite:///./bk.db

# ---- Hugging Face (required if using HF) ----
HF_TOKEN=paste it here
HF_MODEL_ID=google/gemma-2-2b-it
HF_API_BASE=https://api-inference.huggingface.co/models

# ---- Optional: local Ollama (leave empty if using HF) ----
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3:latest

# ---- Optional: OpenAI (leave empty if using HF) ----
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
